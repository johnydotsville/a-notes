* Подумать куда засунуть FetchType. Это вообще может быть отдельной темой?
  Плюс упоминаются какие-то "прокси" при lazy-loading. Что за прокси?

* Отдельно загуглить в интернетах про организацию One-to-one связи,
  про двунаправленность и каскадность сохранения \ удаления.

* em.contains()

* PersistenceUnitUtil util = factory.getPersistenceUnitUtil();
  С помощью нее можно узнавать, загружена ли сущность в память.