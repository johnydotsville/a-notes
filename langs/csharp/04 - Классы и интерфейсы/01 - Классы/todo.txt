модификаторы unsafe, extern, new

Вложенные классы

Статические поля
Статические методы

Передача параметров по ссылке и по значению, out, ref и т.д. >>> внести в конспект про типы данных, потому что к классам это прямого отношения не имеет

Мб порядок инициализации вынести в отдельный конспект? 
В каждой отдельной теме упоминать мельком, либо вообще не упоминать, а потом в отдельном документе собрать все элементы разом.