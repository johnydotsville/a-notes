Обязательно получше расписать как запустить постгрю в докере, со всеми параметрами,
а также как потом ИЗ КОНСОЛИ подключить к контейнеру, отправлять команды на создание,
выборку и т.д.

Как создать пользователя, например при создании контейнера. В общем, чтобы был не
только дефолтный postgres, но и с вменяемым именем.