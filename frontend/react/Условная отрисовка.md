Просто для наглядности

```react
return (
    <div className="App">
      <PostForm createNewPost={createPost} />
      {
        posts.length != 0  // <-- В зависимости от условия рисуем одно или другое
          ? <PostList title={"Посты"} posts={posts} remove={removePost} />
          : <div>Пока нет ни одного поста.</div>
      }
      
    </div>
  );
```

